# ******************************************************************
# This workflow will trigger once a PR opens to main
# ******************************************************************

name: SDK Testing

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: macos-26
    steps:
      - name: Select Xcode 26
        run: sudo xcode-select -s /Applications/Xcode_26.0.app && xcrun simctl list devicetypes

      - name: Branch Checkout
        uses: actions/checkout@v3

      - name: Set up ruby env
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.7.6
          bundler-cache: true

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            Pods
            ~/.cocoapods
          key: ${{ runner.os }}-pods-test-apps-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-test-apps

      - name: CocoaPod Install
        run: pod install

      - name: Run iOS Unit Testing for Dev SDK
        run: bundle exec fastlane test
